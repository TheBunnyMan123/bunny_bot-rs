# TheBunnyBot but Rust!
## Building
### With Nix
To build this with nix, enter the root directory and run `nix build`

### With cargo
To build this with cargo, enter the root directory and run `cargo build --release`

